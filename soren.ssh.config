Host github.com
    # Strength: Github needs diffie-hellman-group-exchange-sha1 some of the time but not always..
    #KexAlgorithms curve25519-sha256@libssh.org,diffie-hellman-group-exchange-sha256,diffie-hellman-group-exchange-sha1,diffie-hellman-group14-sha1

Host *
    # These are the defaults as installed by the Ubuntu 14.04 openssh package.
    SendEnv LANG LC_*
    HashKnownHosts yes
    UseKeychain yes
    AddKeysToAgent yes
    IdentityFile ~/.ssh/id_rsa
    IdentityFile ~/.ssh/id_ed25519
    GSSAPIAuthentication yes
    GSSAPIDelegateCredentials no

    # Now for some strength..
    #KexAlgorithms curve25519-sha256@libssh.org,diffie-hellman-group-exchange-sha256
    PasswordAuthentication no
    ChallengeResponseAuthentication no
    PubkeyAuthentication yes
    #Ciphers chacha20-poly1305@openssh.com,aes256-gcm@openssh.com,aes128-gcm@openssh.com,aes256-ctr,aes192-ctr,aes128-ctr
    #MACs hmac-sha2-512-etm@openssh.com,hmac-sha2-256-etm@openssh.com,hmac-ripemd160-etm@openssh.com,umac-128-etm@openssh.com,hmac-sha2-512,hmac-sha2-256,hmac-ripemd160,umac-128@openssh.com

    # Note: The following config forces us to only connect with a strong client key (which may require us to change client keys).
    # We can generate new ones that live up to this config with:
    #  - ssh-keygen -t ed25519 -o -a 100
    #  - ssh-keygen -t rsa -b 4096 -o -a 100
    #HostKeyAlgorithms ssh-ed25519-cert-v01@openssh.com,ssh-rsa-cert-v01@openssh.com,ssh-rsa-cert-v00@openssh.com,ssh-ed25519,ssh-rsa
### Make sure that .dotfiles follow me around ..
#Host github.com
 # LocalCommand ssh

#Host *
   #PermitLocalCommand yes
   #LocalCommand tar -cz -C${HOME} .oh-my-zsh .zshrc .vim .vimrc | ssh -o PermitLocalCommand=no %n "tar -mxz -C${HOME} ; chsh -s /bin/zsh"
