### Make sure that .dotfiles follow me around ..
Host *
   PermitLocalCommand yes
   LocalCommand tar -cz -C${HOME} .bashrc .oh-my-zsh .zshrc .vim .vimrc \
               | ssh -o PermitLocalCommand=no %n "tar mxz -C${HOME} ; chsh -s /bin/zsh"
